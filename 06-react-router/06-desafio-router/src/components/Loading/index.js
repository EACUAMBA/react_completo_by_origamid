import React from 'react';
import './index.css';
import Circle from "../circle";

const Loading = () => {
    return (
        <div className={'loading'}>
            <Circle/>
        </div>
    );
};

export default Loading;
